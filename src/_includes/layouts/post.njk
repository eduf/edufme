---
layout: base.njk
---

<div class="container">
  <article class="post-single post-type-{{ type }}">

    {% if title %}
      {% if type == "link" %}
        <h1 class="post-title"><a href="{{ url }}" target="_blank" rel="noopener">{{ title }}</a></h1>
      {% else %}
        <h1 class="post-title">{{ title }}</h1>
      {% endif %}
    {% endif %}

    <div class="post-meta">
      <time datetime="{{ page.date | dateISO }}">{{ page.date | dateReadable }}</time>
      <a href="/sobre" class="post-author">{{ site.author }}</a>
    </div>

    {% set socialPost = posse[page.url] %}
    {% if socialPost and (socialPost.mastodonUrl or socialPost.blueskyUrl) %}
      <div class="post-social-links" aria-label="conversas sobre este texto">
        {% if socialPost.mastodonUrl %}
          <a href="{{ socialPost.mastodonUrl }}" target="_blank" rel="noopener">comentar no Mastodon</a>
        {% endif %}
        {% if socialPost.blueskyUrl %}
          <a href="{{ socialPost.blueskyUrl }}" target="_blank" rel="noopener">comentar no Bluesky</a>
        {% endif %}
      </div>
    {% endif %}

    {% if coverImage %}
      <div class="post-cover">
        <img src="{{ coverImage }}" alt="{{ title }}">
      </div>
    {% endif %}

    <div class="post-content">
      {% if type == "quote" %}
        <blockquote>{{ quote | markdownInline | safe }}</blockquote>
        {% if source %}<p class="quote-source">{{ source | markdownInline | safe }}</p>{% endif %}
        {% if content %}{{ content | safe }}{% endif %}
      {% elif type == "link" %}
        {% if content %}{{ content | safe }}{% endif %}
      {% elif type == "image" %}
        <figure>
          <img src="{{ image }}" alt="{{ alt if alt else title }}">
          {% if caption %}
            <figcaption>{{ caption | markdownInline | safe }}</figcaption>
          {% endif %}
        </figure>
        {% if content %}{{ content | safe }}{% endif %}
      {% elif type == "video" %}
        <p><a href="{{ url }}" target="_blank" rel="noopener">{{ url }}</a></p>
        {% if content %}{{ content | safe }}{% endif %}
      {% else %}
        {{ content | safe }}
      {% endif %}
    </div>

    {% include "partials/post-tags.njk" %}

  </article>
</div>
